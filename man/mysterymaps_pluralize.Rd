% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pluralize.R
\name{mysterymaps_pluralize}
\alias{mysterymaps_pluralize}
\title{Format a count with a noun that agrees with it}
\usage{
mysterymaps_pluralize(
  n,
  singular,
  plural = paste0(singular, "s"),
  na_text = "—"
)
}
\arguments{
\item{n}{\code{numeric}: counts.}

\item{singular}{\code{character(1)}: noun for exactly one.}

\item{plural}{\code{character(1)}: noun for any other count. Defaults to
\code{paste0(singular, "s")}, which is wrong often enough ("midwife",
"OB/GYN") that it is worth passing explicitly.}

\item{na_text}{\code{character(1)}: returned when \code{n} is \code{NA}. Default \code{"—"}.}
}
\value{
\code{character} the same length as \code{n}.
}
\description{
Returns "1 midwife", "3 midwives", "1,980 births" — a count formatted with
thousands separators and a noun in the number that matches it. Written for
map tooltips and popups, where a count and its noun sit inches from the
reader's eye and "1 midwives" reads as carelessness about the underlying
data.
}
\details{
\itemize{
\item \strong{Vectorised} over \code{n}, so it drops straight into an \code{sprintf()} building
labels for a whole layer.
\item \strong{\code{NA} is not zero.} A missing count returns \code{na_text} (an em dash by
default) with no noun, because "NA midwives" and "0 midwives" mean
different things and a map that conflates them is making a claim the data
does not support.
\item Counts are rounded before formatting: three-year averages and
area-weighted allocations arrive fractional, and "1,980.3 births" in a
tooltip invites a precision the estimate does not have.
}
}
\examples{
mysterymaps_pluralize(c(0, 1, 3, NA), "midwife", "midwives")
# "0 midwives" "1 midwife" "3 midwives" "—"

mysterymaps_pluralize(1980, "birth")
# "1,980 births"
}
\seealso{
Other map-labels: 
\code{\link{mysterymaps_format_credentials}()},
\code{\link{mysterymaps_place_title_case}()}
}
\concept{map-labels}
