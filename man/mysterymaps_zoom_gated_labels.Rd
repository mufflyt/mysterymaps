% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coverage_surfaces.R
\name{mysterymaps_zoom_gated_labels}
\alias{mysterymaps_zoom_gated_labels}
\title{Show point labels only where they are legible}
\usage{
mysterymaps_zoom_gated_labels(map, group, min_zoom = 9, max_labels = 400)
}
\arguments{
\item{map}{A leaflet map.}

\item{group}{Name of the marker group to label.}

\item{min_zoom}{Zoom at which labels appear. Default 9.}

\item{max_labels}{Cap on simultaneous labels. Default 400.}
}
\value{
The leaflet map with the handler attached.
}
\description{
Permanent tooltips on a large point layer are unreadable at national zoom and
essential at street zoom. This opens tooltips only for markers currently in
view, only at or above \code{min_zoom}, and only up to \code{max_labels} of them.
}
\details{
Marker clustering is the usual answer to a crowded point layer and is a poor
one: it replaces the data with a count and forces repeated zooming before the
reader learns anything. Drawing every point on a canvas renderer and gating
the \emph{labels} keeps the distribution visible at every zoom.
}
\seealso{
Other coverage-surfaces: 
\code{\link{mysterymaps_add_coverage_surfaces}()},
\code{\link{mysterymaps_base_legend_switcher}()},
\code{\link{mysterymaps_register_base_legend}()}
}
\concept{coverage-surfaces}
