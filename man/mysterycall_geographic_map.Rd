% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geographic_map.R
\name{mysterycall_geographic_map}
\alias{mysterycall_geographic_map}
\title{State-Level Choropleth Map of Acceptance Rates}
\usage{
mysterycall_geographic_map(
  data,
  state_col = "state",
  outcome_col = "offered",
  fill_label = "Acceptance rate",
  title = NULL,
  subtitle = NULL,
  palette = c("viridis", "magma", "plasma", "inferno", "cividis"),
  direction = 1L,
  low_states_warn = 5L,
  na_color = "grey80",
  include_alaska_hawaii = FALSE
)
}
\arguments{
\item{data}{data.frame. Must contain at least \code{state_col} and \code{outcome_col}.}

\item{state_col}{Character. Column containing state abbreviations (e.g.
\code{"CO"}, \code{"CA"}) or full state names (e.g. \code{"Colorado"}). Default
\code{"state"}.}

\item{outcome_col}{Character. Column containing either binary 0/1 outcomes
or per-observation acceptance rates (0-1 numeric). Default \code{"offered"}.}

\item{fill_label}{Character. Legend title shown on the fill scale. Default
\code{"Acceptance rate"}.}

\item{title}{Character or \code{NULL}. Plot title. Default \code{NULL}.}

\item{subtitle}{Character or \code{NULL}. Plot subtitle. Default \code{NULL}.}

\item{palette}{Character. Viridis color palette. One of \code{"viridis"},
\code{"magma"}, \code{"plasma"}, \code{"inferno"}, or \code{"cividis"}. Default \code{"viridis"}.}

\item{direction}{Integer. Viridis scale direction: \code{1} (low color = low
value; default) or \code{-1} (reversed).}

\item{low_states_warn}{Integer. Issue a \code{\link[base:warning]{base::warning()}} for any state
with fewer than this many non-missing observations. Default \code{5L}.}

\item{na_color}{Character. Fill color for states not present in \code{data}.
Default \code{"grey80"}.}

\item{include_alaska_hawaii}{Logical. When \code{FALSE} (default), Alaska (\code{AK})
and Hawaii (\code{HI}) are dropped before aggregation and mapping, producing a
standard CONUS choropleth.}
}
\value{
A \code{ggplot} object of class \code{c("gg", "ggplot")}, returned invisibly.
Print or assign the return value to display the map.
}
\description{
Draws a CONUS (or full US) choropleth of per-state appointment acceptance
rates using \pkg{ggplot2}, \pkg{maps}, and \pkg{viridis}. Binary 0/1
outcome columns are aggregated to per-state means automatically; columns
already containing rates (0-1) are used directly.
}
\section{State identifier format}{

The function detects whether \code{state_col} contains abbreviations or full
names by sampling the first non-missing value. When the value is two
characters or fewer it is treated as an abbreviation and looked up in
\link[base:state.abb]{base::state.abb} / \link[base:state.name]{base::state.name}. Longer values are lower-cased
and joined directly to the map polygon data. DC and US territories are not
present in the built-in datasets and will appear as no-data states.
}

\section{Outcome aggregation}{

When every non-missing value in \code{outcome_col} is exactly 0 or 1, the column
is treated as binary and aggregated to a per-state acceptance rate via
\code{\link[base:tapply]{base::tapply()}} (\code{mean(x, na.rm = TRUE)}). Any other numeric column is
also averaged per state, so passing a pre-computed rate works as expected.
}

\examples{
\dontrun{
# --- Binary outcome example (0/1 call outcomes per row) --------------------
set.seed(42)
n <- 300
df <- data.frame(
  state   = sample(c("CO", "CA", "TX", "NY", "FL", "WA", "OR"), n,
                   replace = TRUE),
  offered = rbinom(n, 1, 0.60),
  stringsAsFactors = FALSE
)
p <- mysterycall_geographic_map(
  df,
  title    = "Appointment Acceptance Rate by State",
  subtitle = "Mystery-caller study, n = 300 calls"
)
print(p)

# --- Pre-aggregated rates (one row per state) ------------------------------
rate_df <- data.frame(
  state = c("CO", "CA", "TX", "NY", "FL"),
  rate  = c(0.55, 0.72, 0.48, 0.63, 0.81),
  stringsAsFactors = FALSE
)
mysterycall_geographic_map(
  rate_df,
  outcome_col = "rate",
  palette     = "plasma",
  direction   = -1L,
  fill_label  = "Acceptance\nrate"
)
}
}
\seealso{
\code{\link[=mysterycall_map_acceptance_rate]{mysterycall_map_acceptance_rate()}} for a simpler one-call
choropleth; \code{\link[=mysterycall_forest_plot]{mysterycall_forest_plot()}} for model-level visualization.

Other reporting: 
\code{\link{mysterycall_abstract_numbers}()},
\code{\link{mysterycall_exclusion_summary}()},
\code{\link{mysterycall_irr_to_days}()},
\code{\link{mysterycall_results_paragraph}()},
\code{\link{mysterycall_session_snapshot}()},
\code{\link{mysterycall_supplemental_tables}()},
\code{\link{mysterycall_write_results_paragraph}()},
\code{\link{print.mysterycall_abstract_numbers}()},
\code{\link{print.mysterycall_irr_days}()},
\code{\link{print.mysterycall_snapshot}()}
}
\concept{reporting}
