% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coverage_gates.R
\name{mysterymaps_gate_provider_coverage}
\alias{mysterymaps_gate_provider_coverage}
\title{Assert every provider falls inside the coverage surface built from them}
\usage{
mysterymaps_gate_provider_coverage(
  providers,
  surface,
  label = "coverage",
  max_missing_pct = 0,
  group_col = NULL,
  on_fail = c("error", "warn")
)
}
\arguments{
\item{providers}{\link[sf:sf]{sf::sf}: provider points.}

\item{surface}{\link{sf|sfc}: the dissolved coverage polygon(s).}

\item{label}{\link{character(1)}: band name for the message, e.g. "30-minute".}

\item{max_missing_pct}{\link{numeric(1)}: tolerated share outside, as a
percentage. Defaults to 0 — a provider outside their own isochrone is a
defect, not a rounding error.}

\item{group_col}{\link{character(1)|NULL}: column to break the report down by
(a state column makes a clustered failure obvious immediately).}

\item{on_fail}{\code{"error"} (default) or \code{"warn"}.}
}
\value{
Invisibly a list with \code{n}, \code{n_outside}, \code{pct_outside} and, when
\code{group_col} is given, \code{by_group}.
}
\description{
A drive-time surface is dissolved from per-provider isochrones, so every
provider must lie inside it: a provider is always within 30 minutes of
themselves. Any who do not are providers whose isochrone is missing, and the
surface understates access by exactly that much.
}
\details{
This fails quietly without a gate. A missing isochrone does not error, does
not warn, and does not leave a hole you would notice — it simply removes
shading that should have been there, and the map still looks plausible. On
the midwifery access map 490 of 11,792 midwives sat outside their own
30-minute surface, concentrated in Missouri (117), Iowa (112) and Kansas
(79). Clustered like that, the loss is not random: it biases the coverage
gap toward exactly the regions the map exists to describe.
}
\seealso{
Other county-access-template: 
\code{\link{mysterymaps_county_access_map}()},
\code{\link{mysterymaps_guard_water_masks}()},
\code{\link{mysterymaps_name_search}()},
\code{\link{mysterymaps_notes_panel}()}
}
\concept{county-access-template}
