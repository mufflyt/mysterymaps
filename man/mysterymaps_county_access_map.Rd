% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/county_access_map.R
\name{mysterymaps_county_access_map}
\alias{mysterymaps_county_access_map}
\title{County choropleth with drive-time coverage surfaces}
\usage{
mysterymaps_county_access_map(
  counties,
  value_col,
  label_col = NULL,
  popup_col = NULL,
  coverage = NULL,
  coverage_colors = NULL,
  coverage_labels = NULL,
  coverage_titles = "Coverage",
  points = NULL,
  point_group = "Points",
  legend_title = value_col,
  value_group = NULL,
  search = NULL,
  notes = NULL,
  bounds = c(24.5, -125, 49.4, -66.9),
  jenks_k = 6,
  zero_label = "none"
)
}
\arguments{
\item{counties}{\code{sf}: polygons carrying the value, label and popup columns.}

\item{value_col}{\code{character(1)}: numeric column driving the choropleth.}

\item{label_col, popup_col}{\code{character(1)}: character columns of pre-rendered
HTML for hover and click. \code{NULL} to omit either.}

\item{coverage}{Named \code{list} of \code{sf} coverage surfaces; names become layer
names. \code{NULL} for a choropleth-only map.}

\item{coverage_colors, coverage_labels, coverage_titles}{\code{character}: passed
through to \code{\link[=mysterymaps_add_coverage_surfaces]{mysterymaps_add_coverage_surfaces()}}.}

\item{points}{\code{sf}/\code{data.frame} or \code{NULL}: markers to add inside the
template. Usually \code{NULL} — add them afterwards when they need their own
popups.}

\item{point_group}{\code{character(1)}: group name used when \code{points} is given.}

\item{legend_title}{\code{character(1)}: choropleth legend title; may contain HTML.}

\item{value_group}{\code{character(1)}: layer name for the choropleth. Defaults to
\code{legend_title} with tags stripped.}

\item{search, notes}{Reserved for callers that pass them; \code{NULL} skips.}

\item{bounds}{\code{numeric(4)}: \code{c(lat_min, lng_min, lat_max, lng_max)}.}

\item{jenks_k}{\code{integer(1)}: classes for the positive values. Default 6.}

\item{zero_label}{\code{character(1)}: legend text for the zero class.}
}
\value{
A leaflet widget.
}
\description{
Assembles the map shape this ecosystem keeps rebuilding: a county choropleth
of a supply measure, one or more dissolved drive-time surfaces as mutually
exclusive alternatives to it, a legend that follows the active layer, and
CONUS framing. The midwifery access map and the urogyn county map each
hand-built this separately before it lived here.
}
\details{
What the template owns, so callers stop re-deriving it:
\itemize{
\item canvas rendering (\code{preferCanvas}), which large point layers need;
\item a \code{"pts"} map pane at \code{zIndex} 650, so markers added afterwards
hit-test above the choropleth instead of the polygon swallowing clicks;
\item an imperial scale bar bottom-left, leaving bottom-right for legends;
\item \code{\link[=mysterymaps_jenks_zero_scale]{mysterymaps_jenks_zero_scale()}}, so zero is its own class rather
than the bottom of a gradient;
\item a single always-on county mesh under the coverage layers — binding
one copy per group triples the geometry in the HTML;
\item base-group legends via \code{\link[=mysterymaps_add_coverage_surfaces]{mysterymaps_add_coverage_surfaces()}} and
\code{\link[=mysterymaps_base_legend_switcher]{mysterymaps_base_legend_switcher()}}, because
\code{leaflet::addLegend(group=)} silently does nothing for \code{baseGroups};
\item label tiles drawn last, so place names sit above the fills.
}

The caller supplies data and vocabulary. Anything map-specific — a notes
panel, per-point popups, zoom-gated labels — is added to the returned widget
afterwards.
}
\seealso{
Other map-templates: 
\code{\link{mysterymaps_name_search}()}
}
\concept{map-templates}
