% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format_credentials.R
\name{mysterymaps_format_credentials}
\alias{mysterymaps_format_credentials}
\title{Tidy a raw NPPES credential string for display}
\usage{
mysterymaps_format_credentials(x, sep = ", ")
}
\arguments{
\item{x}{\code{character}: raw credential strings.}

\item{sep}{\code{character(1)}: separator for the rejoined tokens. Default \code{", "}.}
}
\value{
\code{character} the same length as \code{x}; \code{NA} where the input was \code{NA},
empty, or held no usable token.
}
\description{
Turns the free text NPPES stores in \code{credential} into something fit for a map
label: \code{"C.N.M."}, \code{"CNM"}, \code{"RN, CNM"} and \code{"APRN-CNM"} are all written by
hand by the provider, and a tooltip that shows them verbatim looks like the
underlying data was never examined.

Punctuation is stripped, separators are normalised, tokens are upper-cased
and de-duplicated in place, and the result is rejoined with commas.
}
\section{This formats; it does not classify}{

It makes no judgement about what a credential \emph{means} — it will not tell you
whether someone is a nurse-midwife. Classification lives elsewhere and is a
harder problem: see \code{classify_nppes_credential()} and
\code{apply_credential_gate()} in mufflyt/isochrones. Those return a physician
degree (\code{"MD"}, \code{"DO"}, \code{"UNKNOWN"}) and are the wrong tool for midwifery
credentials, which is why this function is a formatter and stops there.

Deliberately NO synonym table. Folding \code{ARNP} into \code{APRN}, or dropping \code{RN}
as redundant beside \code{CNM}, is a substantive claim about credentialing that
belongs in a classifier a domain expert has reviewed — not in a label
helper, where it would silently rewrite what a provider reported.
}

\examples{
mysterymaps_format_credentials(c("C.N.M.", "RN, CNM", "cnm/whnp", NA))
# "CNM" "RN, CNM" "CNM, WHNP" NA

mysterymaps_format_credentials("CNM, C.N.M., CNM")
# "CNM"
}
\seealso{
Other map-labels: 
\code{\link{mysterymaps_place_title_case}()},
\code{\link{mysterymaps_pluralize}()}
}
\concept{map-labels}
