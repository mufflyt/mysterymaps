% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/text_helpers.R
\name{mysterymaps_format_credentials}
\alias{mysterymaps_format_credentials}
\title{Format provider credentials for display after a name}
\usage{
mysterymaps_format_credentials(x, keep = .MM_CRED_KEEP, max_n = 3L)
}
\arguments{
\item{x}{\code{character}: raw credential text, e.g. \code{"RN, CNM"}.}

\item{keep}{\code{character}: credentials to display. Defaults to midwifery,
women's-health nurse-practitioner and physician credentials.}

\item{max_n}{\code{integer(1)}: most credentials to show; extras are dropped
rather than allowed to overrun a popup line.}
}
\value{
\link{character} the same length as \code{x}; \code{NA} where nothing survives.
}
\description{
Turns free-form NPPES credential text into a short, canonical suffix:
\code{"C.N.M."} and \code{"RN, CNM"} both become \code{"CNM"}, and \code{"CNM, WHNP-BC"} is kept
whole.
}
\details{
NPPES credential text is entered by the provider and is not controlled
vocabulary. Across 18,760 midwives it appears as \code{CNM}, \code{C.N.M.}, \code{C.N.M},
\verb{RN, CNM}, \code{APRN-CNM}, \verb{CNM, WHNP-BC}, \verb{MSN, CNM} and dozens more. Rendering
it verbatim in a popup shows the data-entry variation rather than the
credential.

Selection is a KEEP-list, not a drop-list. The text is unbounded, so an
exclusion list quietly admits whatever new string appears next; an inclusion
list fails closed. Degrees and licences that are not credentials in the
honorific sense (\code{RN}, \code{APRN}, \code{ARNP}, \code{MSN}, \code{DNP}, \code{PhD}) are therefore not
shown by default -- pass \code{keep} to change that.

Order follows the source string, so a provider who lists \verb{CNM, WHNP-BC} keeps
that order rather than having one imposed.
}
\examples{
mysterymaps_format_credentials("C.N.M.")        # "CNM"
mysterymaps_format_credentials("RN, CNM")       # "CNM"
mysterymaps_format_credentials("CNM, WHNP-BC")  # "CNM, WHNP-BC"
mysterymaps_format_credentials("RN")            # NA

}
\seealso{
Other text: 
\code{\link{mysterymaps_place_title_case}()},
\code{\link{mysterymaps_pluralize}()}
}
\concept{text}
