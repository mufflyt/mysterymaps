% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jenks_zero_scale.R
\name{mysterymaps_jenks_zero_scale}
\alias{mysterymaps_jenks_zero_scale}
\title{Zero-aware Jenks natural-breaks colour scale}
\usage{
mysterymaps_jenks_zero_scale(
  n,
  k = 6,
  zero_col = "#e0e0e0",
  na_col = "#ffffff",
  na_label = "No data",
  palette = viridisLite::viridis,
  digits = NULL
)
}
\arguments{
\item{n}{Numeric vector of values (counts or rates).}

\item{k}{Target number of classes for the positive values. Default 6.}

\item{zero_col}{Colour for zero. Default light grey.}

\item{na_col}{Colour for \code{NA} and \code{NaN}, which mean unmeasured rather than
none. Default white. Pass \code{zero_col} to restore the pre-0.2.1 behaviour of
shading unmeasured geographies as if they were zero.}

\item{na_label}{Legend entry for \code{na_col}. The entry is added only when the
data actually contains \code{NA}, so legends do not gain an empty category.}

\item{palette}{Palette function taking \code{k}. Default \code{viridisLite::viridis}.}

\item{digits}{Decimal places for continuous labels; \code{NULL} gives integer
count labels.}
}
\value{
A list with \code{color} (a function mapping values to colours),
\code{leg_cols} and \code{leg_labs} for \code{leaflet::addLegend()}.
}
\description{
Classifies POSITIVE values with Jenks natural breaks and gives zero its own
colour and legend entry. Zero is a category, not the low end of a scale: a
county with no provider is categorically different from one with a low rate,
and binning them together hides exactly the finding a workforce map exists to
show. A first draft of a midwifery access map used an equal-interval bottom
bin of 0.0-0.5 and so coloured 1,619 of 3,109 counties -- over half the map --
as "low" when the truth was "none".
}
\details{
Jenks rather than equal intervals because provider rates are heavily
right-skewed; equal intervals put almost every county in the first bin.
}
\examples{
\dontrun{
  sc <- mysterymaps_jenks_zero_scale(counties$rate, k = 6, digits = 1)
  leaflet::addPolygons(map, fillColor = sc$color(counties$rate))
  leaflet::addLegend(map, colors = sc$leg_cols, labels = sc$leg_labs)
}
}
\seealso{
Other coverage-surfaces:
\code{\link[=mysterymaps_add_coverage_surfaces]{mysterymaps_add_coverage_surfaces()}},
\code{\link[=mysterymaps_base_legend_switcher]{mysterymaps_base_legend_switcher()}},
\code{\link[=mysterymaps_register_base_legend]{mysterymaps_register_base_legend()}},
\code{\link[=mysterymaps_zoom_gated_labels]{mysterymaps_zoom_gated_labels()}}
}
\concept{coverage-surfaces}
